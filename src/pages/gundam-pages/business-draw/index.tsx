import React, { useState, useEffect, useRef } from 'react';
import { useModel } from 'umi';
import { Table, Button } from 'antd';
import {} from 'antd';

// 机器人列表
const DetailPages: React.FC = (props: any) => {
  return <div>业务流程管理</div>;
};

export default DetailPages;
