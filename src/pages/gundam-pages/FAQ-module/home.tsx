const FAQModulePages = (props: any) => {
  return <div style={{ width: '100%', height: '100%' }}>{props.children}</div>;
};

export default FAQModulePages;

// clearlist  FAQ澄清模块

// blacklist  FAQ黑名单模块

// pendingList 待审核列表

// reviewedList 带处理列表
